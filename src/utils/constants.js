// src/utils/constants.js
// Define the similarity threshold for matching user queries to the knowledge base
 export const SIMILARITY_THRESHOLD = 0.6; //This value we can be tuned: higher = looser match